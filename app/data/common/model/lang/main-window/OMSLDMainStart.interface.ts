/** Translates of start frame in main window */
export interface IOMSLDMainStart {
  ACTIONS: {
    /** Title of section "Start work" */
    TITLE: string;

    /** Label of link "Add or import platform" */
    ADD_OR_IMPORT_PLATFORM: string;
  };
}
